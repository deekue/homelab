---
prometheus:
  admissionWebhooks:
    enabled: false
    certManager:
      enabled: true
  prometheusSpec:
    resources:
      requests:
        memory: 400Mi
    storageSpec:
      volumeClaimTemplate:
        spec:
          storageClassName: replicated-r2
          accessModes: ["ReadWriteOnce"]
          resources:
            requests:
              storage: 50Gi
