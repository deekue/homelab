---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: loki-pvc
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: replicated-r2-v2
  resources:
    requests:
      storage: 50Gi
